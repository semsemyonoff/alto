services:
  alto:
    image: alto:latest
    build: .
    ports:
      - "8080:8080"
    environment:
      ALTO_LIBRARIES: "Music:/music,Lossless:/lossless"
      ALTO_PORT: "8080"
      ALTO_OUTPUT_DIR: "/out"
      ALTO_DB_PATH: "/data/alto.db"
      ALTO_CACHE_DIR: "/data/cache"
    volumes:
      - /path/to/your/music:/music:ro # :ro is safe for Shared mode only; Local and Replace modes need :rw
      - /path/to/your/lossless:/lossless:ro # same as above
      - /path/to/output:/out
      - alto_data:/data
    restart: unless-stopped

volumes:
  alto_data:
